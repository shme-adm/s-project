name: Deploy to VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Также можно запустить вручную

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache,mode=max

      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          script: |
            set -e
            
            echo "🚀 Starting deployment..."
            
            PROJECT_DIR="/opt/portal-s"
            REPO_URL="${{ github.server_url }}/${{ github.repository }}.git"
            
            # Проверяем, существует ли репозиторий
            if [ ! -d "$PROJECT_DIR/.git" ]; then
                echo "📥 First deployment detected - cloning repository..."
                
                # Очищаем директорию, если она существует
                if [ -d "$PROJECT_DIR" ]; then
                    sudo rm -rf "$PROJECT_DIR"
                fi
                
                # Создаем и клонируем репозиторий
                mkdir -p "$PROJECT_DIR"
                cd "$PROJECT_DIR"
                git clone "$REPO_URL" .
                echo "✅ Repository cloned successfully"
            else
                echo "✅ Repository exists - updating..."
                cd "$PROJECT_DIR"
                git fetch origin main
                git checkout main
                git pull origin main
            fi
            
            # Проверяем существование .env файла
            if [ ! -f ".env" ]; then
                echo "❌ ERROR: .env file not found in $PROJECT_DIR"
                echo "⚠️ First deployment requires .env file to be placed on server"
                exit 1
            fi
            
            echo "✅ .env file found"
            
            # Логируемся в container registry
            if [ ! -z "${{ secrets.GHCR_TOKEN }}" ]; then
                echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            else
                echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            fi
            
            # Создаем необходимые директории
            mkdir -p logs
            mkdir -p client/node_modules
            mkdir -p server/node_modules
            
            # Останавливаем старые контейнеры
            echo "⏹️ Stopping old containers..."
            docker compose down -v || true
            
            # Очищаем неиспользуемые ресурсы
            echo "🧹 Pruning Docker resources..."
            docker system prune -af --volumes || true
            
            # Вытягиваем образы
            echo "📦 Pulling images..."
            docker compose pull || true
            
            # Запускаем контейнеры
            echo "🐳 Starting containers..."
            docker compose up -d --build
            
            # Проверяем здоровье сервиса
            echo "🔍 Checking service health (max 30 attempts)..."
            MAX_ATTEMPTS=30
            ATTEMPT=1
            
            while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
              if curl -f http://localhost:3000/api/health > /dev/null 2>&1; then
                echo "✅ API is healthy"
                break
              fi
              
              if [ $ATTEMPT -eq $MAX_ATTEMPTS ]; then
                echo "⚠️ API health check failed after $MAX_ATTEMPTS attempts"
                echo "📊 Container status:"
                docker compose ps
                break
              fi
              
              echo "⏳ Attempt $ATTEMPT/$MAX_ATTEMPTS..."
              sleep 2
              ATTEMPT=$((ATTEMPT + 1))
            done
            
            echo ""
            echo "📊 Container status:"
            docker compose ps
            
            echo "✅ Deployment completed successfully!"

      - name: Notify on success
        if: success()
        run: |
          echo "✅ Deployment successful!"
          echo "🌐 Application deployed at: https://${{ secrets.VPS_DOMAIN }}"

      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Deployment failed!"
          echo "Check GitHub Actions logs for details"
